# nmap_local_IT_infrastructure
Identified the assets within a local IT infrastructure using the Zenmap application on Windows. Also detected the Operating system running and ports opened on the assets.
